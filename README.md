# نظام إدارة الحلقات القرآنية (MAS)

<div dir="rtl">

## 📖 الوصف

نظام متكامل لإدارة الحلقات القرآنية يشمل:
- إدارة المجمعات والمساجد والحلقات
- إدارة المعلمين والطلاب
- تسجيل الحضور والغياب
- متابعة التسميع والحفظ
- لوحات تحكم متعددة (مشرف، معلم، ولي أمر)

## 🚀 متطلبات التشغيل

1. **حساب Supabase** (مجاني): https://supabase.com
2. **استضافة ويب** (أي استضافة تدعم الملفات الثابتة)

## 📦 خطوات التثبيت

### 1. إعداد قاعدة البيانات

1. أنشئ حساباً مجانياً في [Supabase](https://supabase.com)
2. أنشئ مشروعاً جديداً (New Project)
3. انتظر حتى يكتمل إنشاء المشروع
4. اذهب إلى **SQL Editor** من القائمة الجانبية
5. انسخ محتوى الملف `database/schema-production.sql`
6. الصقه في SQL Editor واضغط **Run**

### 2. الحصول على مفاتيح الاتصال

1. في Supabase، اذهب إلى **Settings > API**
2. انسخ **Project URL** (يبدأ بـ https://xxx.supabase.co)
3. انسخ **anon public** key

### 3. تحديث إعدادات التطبيق

1. افتح الملف `js/config.js`
2. استبدل القيم التالية:

```javascript
SUPABASE_URL: 'https://YOUR_PROJECT_ID.supabase.co',  // ← ضع Project URL هنا
SUPABASE_ANON_KEY: 'YOUR_ANON_KEY_HERE',              // ← ضع anon key هنا
```

3. غيّر `IS_DEVELOPMENT` إلى `false`:

```javascript
IS_DEVELOPMENT: false,
```

### 4. رفع الملفات

ارفع جميع ملفات المشروع إلى الاستضافة:
- جميع ملفات `.html`
- جميع ملفات `.css`
- جميع ملفات `.js`
- مجلد `js/`

### 5. إنشاء أول مستخدم

1. افتح الموقع في المتصفح
2. اذهب لصفحة `create-complex.html`
3. أنشئ مجمعاً ومشرفاً جديداً
4. سجّل الدخول باستخدام البيانات التي أدخلتها

## 📁 هيكل الملفات

```
MAS/
├── index.html          # الصفحة الرئيسية
├── login.html          # صفحة تسجيل الدخول
├── create-complex.html # إنشاء مجمع جديد
├── dashboard.html      # لوحة تحكم المعلم
├── supervisor-dashboard.html  # لوحة تحكم المشرف
├── parent-dashboard.html      # لوحة تحكم ولي الأمر
├── students.html       # إدارة الطلاب
├── teachers.html       # إدارة المعلمين
├── halaqat.html        # إدارة الحلقات
├── attendance.html     # تسجيل الحضور
├── recitation.html     # تسجيل التسميع
├── js/
│   ├── config.js       # ⚠️ إعدادات التطبيق (يجب تحديثه)
│   └── supabase-db.js  # الاتصال بقاعدة البيانات
└── database/
    ├── schema.sql      # قاعدة بيانات التطوير
    └── schema-production.sql  # ⚠️ قاعدة بيانات الإنتاج
```

## 👥 أدوار المستخدمين

| الدور | الصلاحيات |
|-------|----------|
| **مشرف** (supervisor) | إدارة كاملة: المعلمين، الطلاب، الحلقات |
| **معلم** (teacher) | تسجيل الحضور والتسميع للحلقات المسندة |
| **ولي أمر** (parent) | متابعة أبنائه فقط |

## 🔒 الأمان

- تم تفعيل **Row Level Security (RLS)** في قاعدة البيانات
- مفتاح `anon` آمن للاستخدام في الواجهة الأمامية
- البيانات محمية بسياسات الوصول

## ⚠️ ملاحظات مهمة

1. **لا تشارك** ملف `config.js` مع أي شخص
2. **تأكد** من تغيير `IS_DEVELOPMENT` إلى `false` قبل النشر
3. **احتفظ** بنسخة احتياطية من قاعدة البيانات بانتظام
4. **استخدم** كلمات مرور قوية للمستخدمين

## 📞 الدعم الفني

للمساعدة أو الاستفسارات، تواصل مع فريق التطوير.

## 📄 الترخيص

جميع الحقوق محفوظة © 2026

</div>
